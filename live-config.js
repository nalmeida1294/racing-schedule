// Disable the experimental direct feed here if the provider changes access.
// No API keys, passwords, or backend are used by this connection.
window.RACE_LIVE_CONFIG = Object.freeze({ enabled: true });
